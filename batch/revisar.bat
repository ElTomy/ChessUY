if "%username%"=="Sven Quiring" (
    if "%1"=="admin" (
        call admin\admin.bat
        )
    )
if "%username%"=="Sven Quiring" (
    if "%1"=="tecdered" (
        call tecdered\tecdered.bat
        )
    )
if "%username%"=="Sven Quiring" (
    if "%1"=="tecbd" (
        call tecbd\tecbd.bat
        )
    )
